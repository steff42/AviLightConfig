<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project default="create_run_jar" name="Create Runnable Jar for Project AviLightConfig with Jar-in-Jar Loader">
    <!--this file was created by Eclipse Runnable JAR Export Wizard-->
    <!--ANT 1.7 is required                                        -->
    <target name="create_run_jar">
        <jar destfile="${basedir}/executable/AviLightConfig.jar">
            <manifest>
                <attribute name="Main-Class" value="org.eclipse.jdt.internal.jarinjarloader.JarRsrcLoader"/>
                <attribute name="Rsrc-Main-Class" value="de.prim.avilight.Start"/>
                <attribute name="Class-Path" value="."/>
                <attribute name="Rsrc-Class-Path" value="./ jaxb-xjc.jar RXTXcomm.jar"/>
            </manifest>
            <zipfileset src="jar-in-jar-loader.zip"/>
            <fileset dir="${basedir}/bin"/>
            <zipfileset dir="${basedir}/lib" includes="jaxb-xjc.jar"/>
            <zipfileset dir="${basedir}/lib" includes="RXTXcomm.jar"/>
        </jar>
        <zip destfile="${basedir}/AviLightConfig.zip" 
        	basedir="${basedir}/executable">
        	<fileset dir="." includes="AviLightConfig.jar"/>
        	<fileset dir="${basedir}/batchfiles" includes="*.cmd"/>
        	<zipfileset dir="${basedir}/lib/32" prefix="32"/>
        	<zipfileset dir="${basedir}/lib/64" prefix="64"/>
        </zip>
    </target>
</project>
